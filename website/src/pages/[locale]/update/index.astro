---
import BaseLayout from '../../../layouts/BaseLayout.astro'
import { t, type Locale, hrefFor, locales } from '../../../i18n'

export function getStaticPaths() {
  return locales
    .filter((l) => l !== 'en')
    .map((locale) => ({ params: { locale } }))
}

const { locale } = Astro.params as { locale: Locale }
const canonicalPath = hrefFor(locale, '/update/')
---

<BaseLayout lang={locale} title={t(locale, 'meta.title')} description={t(locale, 'meta.description')} canonicalPath={canonicalPath}>
  <main style="max-width: 860px; margin: 0 auto; padding: 64px 20px;">
    <h1 style="margin: 0 0 12px; font-size: 34px; line-height: 1.1; color: rgba(233,238,252,0.98);">
      Update
    </h1>
    <p style="margin: 0; font-size: 18px; color: rgba(233,238,252,0.85);">
      Автообновление недоступно, самостоятельно удалите приложение и скачайте новое 😁
    </p>
  </main>
</BaseLayout>


